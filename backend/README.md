# Backend

FastAPI backend will be added after the base development environment is verified.

Target structure:

backend/
  app/
    api/
    core/
    models/
    schemas/
    repositories/
    services/
    workers/
    utils/
    main.py
  tests/
